// Signals, events, string interpolation.
import { signal } from "bloom"

component Counter {
  let count = signal(0)

  div(class: "counter") {
    p { "Count: {count()}" }
    button(onClick: () => count(count() + 1)) { "Increment" }
    button(onClick: () => count(0)) { "Reset" }
  }
}
