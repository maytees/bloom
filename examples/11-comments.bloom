// line comment at file top
/* block comment
   spanning multiple lines */
import { signal } from "bloom"

component Commented {
  let x = signal(0) // trailing line comment in script zone

  /* block comment in script zone */
  div {
    // line comment in view zone
    p { "x is {x()}" } /* trailing block comment after a child */
  }
}
